local options = {
	ensure_installed = { "lua" },

	highlight = {
		-- enable = true,
		use_languagetree = true,
    additional_vim_regex_highlighting = false,
	},

	indent = { enable = true },
}

return options
